.env
.venv/
__pycache__/
*.pyc

# 產出檔
outputs/

# Google 憑證與登入權杖 —— 絕對不要上傳
data/google_credentials.json
data/google_token.json
data/

# LINE 語料是同學之間的私人對話，人名不會遮蔽 —— 不進版控。
# 需要讓部署版也讀得到的話，自己確認過內容再把這行註解掉。
knowledge/語料/

# knowledge/雲端文件/ 有進版控（repo 是 private）。
# 匯入時已經擋掉名冊：103 份只保留欄位結構，手機/市話/信箱/身分證/學號全部遮蔽。
# 但企劃書、會議紀錄的正文裡仍有幹部與講師姓名 —— 這是社團自己的工作紀錄，
# 沒辦法在不破壞文件價值的前提下拿掉。所以這個 repo 不可以轉成 public。
