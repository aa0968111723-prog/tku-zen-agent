# ── 淡江大學領袖禪學社 AI 代理 · 設定檔範本 ──────────────────────
# 用法：把這個檔案複製一份改名為 .env，再把下面的值填進去。
#      .env 不會被上傳、不會被 git 追蹤，金鑰只留在你自己電腦上。

# ── 1. NVIDIA Build 免費模型（必填）────────────────────────────
# 到 https://build.nvidia.com/settings/api-keys 產生金鑰（免費、不用信用卡）
# 金鑰長得像 nvapi-xxxxxxxxxxxxxxxxxxxx
NVIDIA_API_KEY=nvapi-請填入你的金鑰

# OpenAI 相容端點，通常不用改
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1

# 主力模型：要支援 function calling（工具呼叫），否則代理無法「動手做」
# 已驗證可用的選項：
#   meta/llama-3.1-70b-instruct          ← 穩，預設
#   nvidia/llama-3.3-nemotron-super-49b-v1.5
#   moonshotai/kimi-k2-instruct
#   qwen/qwen2.5-72b-instruct
#   mistralai/mixtral-8x22b-instruct
NVIDIA_MODEL=meta/llama-3.1-70b-instruct

# ── 2. 產出落點 ──────────────────────────────────────────────
# local = 只存本機 outputs 資料夾
# drive = 只上傳 Google 雲端硬碟
# both  = 兩邊都要
DEFAULT_DESTINATION=local

# 本機產出資料夾（相對於專案根目錄，或填絕對路徑）
OUTPUT_DIR=outputs

# ── 3. Google 雲端硬碟（選用，只有要 drive/both 才需要）──────────
# 「淡大領共用雲端」的資料夾 ID
DRIVE_FOLDER_ID=1H-GuCfVw51D5_ipAoivjboabhb7iaKt6
# OAuth 憑證檔位置（見 README「雲端落點」的設定步驟）
GOOGLE_CREDENTIALS_FILE=data/google_credentials.json
GOOGLE_TOKEN_FILE=data/google_token.json

# ── 4. LINE 語料（預設關閉）──────────────────────────────────
# 設成 true 才會把 D:\對話 的 LINE 記錄納入代理可檢索範圍。
# 注意：開啟後，被檢索到的對話片段會隨提問送到 NVIDIA 的伺服器。
# 匯入時會自動遮蔽電話、信箱、學號，但人名不會遮蔽。
ENABLE_LINE_CORPUS=false

# ── 5. 伺服器 ────────────────────────────────────────────────
HOST=127.0.0.1
PORT=8848
